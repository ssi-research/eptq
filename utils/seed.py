# import random
# import numpy as np
# import torch
# import tensorflow as tf


def set_seed(seed: int):
    pass
    # torch.random.manual_seed(seed)
    # tf.random.set_seed(seed)
    # np.random.seed(seed)
    # random.seed(seed)
